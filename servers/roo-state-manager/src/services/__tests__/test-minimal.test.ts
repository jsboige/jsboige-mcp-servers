import { test, expect } from 'vitest';

test('RooSettingsService test file syntax check', () => {
  // Ce test vérifie simplement que le fichier de test peut être importé
  expect(true).toBe(true);
});